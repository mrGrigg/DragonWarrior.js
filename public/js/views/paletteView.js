define(function(require) {
    var view;

    view = Backbone.View.extend({
        id: 'palette'

        , initialize: function() {

        }

        , render: function() {

            return this;
        }
    });

    return view;
});
